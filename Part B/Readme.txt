HomePage.html,Destinations.html and index.html  DESCRIPTION:-


Description :-This is a travel agency website .In this we have two options Home and destination .After clicking on Home it will go to Home page and after destination page it will go to Destination .


Homepage.html :Home and Destination headers are present .
Destinations.html:it will contain audio and video tag .
Index.html:It contains information about the home page.

-----------------------------------------------------------------------------------------------------------
TAGS EXPLANATION :-


<a>:The <a> tag defines a hyperlink, which is used to link from one page to another.
<body>:This tag defines the document's body.The element contains all the contents of an HTML document, such as headings, paragraphs, images, hyperlinks, tables, lists, etc.
<br>:The <br> tag inserts a single line break.
<!DOCTYPE html>:All HTML documents must start with a <!DOCTYPE> declaration.The declaration is not an HTML tag. It is an "information" to the browser about what document type to expect.
<div>:The <div> tag defines a division or a section in an HTML document.
</footer>:A <footer> element typically contains:
authorship information
copyright information
contact information
sitemap
back to top links
related documents
</form>:The <form> tag is used to create an HTML form for user input.
href:href attribute, it is only a placeholder for a hyperlink.
<html>:The <html> tag represents the root of an HTML document.
<h2>:An H2 tag marks the first sub-heading after document's main heading
<header>:The <header> element represents a container for introductory content or a set of navigational links.:-
one or more heading elements (<h1> - <h6>)
logo or icon
authorship information
<img>:The <img> tag is used to embed an image in an HTML page.
<title>:The <title> tag defines the title of the document. The title must be text-only, and it is shown in the browser's title bar or in the page's tab.
<link>:The <link> tag defines the relationship between the current document and an external resource.
<li>:The <li> tag defines a list item.
<label>:The <label> tag defines a label for several elements:

<input type="checkbox">
<input type="color">
<input type="date">
<input type="datetime-local">
<input type="email">
<input type="file">
<input type="month">
<input type="number">
<input type="password">
<input type="radio">
<input type="range">
<input type="search">
<input type="tel">
<input type="text">
<input type="time">
<input type="url">
<input type="week">
<meter>
<progress>
<select>
<textarea>

<main>:The <main> tag specifies the main content of a document.
<nav>:The <nav> tag defines a set of navigation links.
<p>:The <p> tag defines a paragraph.
</section>:The <section> tag defines a section in a document.
<table>:The <table> tag defines an HTML table.An HTML table consists of one <table> element and one or more <tr>, <th>, and <td> elements.
 <tr>:The <tr> element defines a table row
<td>:the <td> element defines a table cell.
<th>:the <th> element defines a table header,
<ul>:The <ul> tag defines an unordered (bulleted) list.

